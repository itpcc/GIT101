//@TODO eiei
function add(x, y){
  if(isNaN(x) || isNaN(y)) return null;
  return x+y;
}
function sub(x, y){
  if(isNaN(x) || isNaN(y)) return null;
  return x-y;
}
function multiply(x, y){
  if(isNaN(x) || isNaN(y)) return null;
  return x*y;
}
